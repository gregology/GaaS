# Draft Review Comment

You are formatting a review of PR #{{ pr_number }}: **{{ pr_title }}** as a GitHub comment.

## Analysis summary

{{ analysis.summary }}

## Findings

{% if findings %}
{% for f in findings %}
### {{ f.severity | upper }}: {{ f.category }} — `{{ f.file }}`{% if f.line %} (line {{ f.line }}){% endif %}

{{ f.description }}

**Suggestion:** {{ f.suggestion }}

{% endfor %}
{% else %}
No findings — the PR looks clean.
{% endif %}

## Instructions

Format this review as a single markdown comment suitable for posting on a GitHub pull request.

Structure the comment as:

1. A brief summary of what the PR does (1-2 sentences, from the analysis)
2. The findings grouped by severity (critical first, then warnings, then notes)
3. For each finding: the file and line, what the issue is, and the suggested fix
4. A short closing line with your overall impression (clean, needs minor fixes, has blockers, etc.)

If there are no findings, write a short comment acknowledging the PR is clean and noting what you checked.

Use markdown formatting appropriate for GitHub (code blocks, file references with backticks, etc.).

{% include "_personality.md.j2" %}

Your structured output `body` field must contain the **full markdown text** of the comment to post. Do not return a URL, reference, or summary — return the complete comment body ready to be posted verbatim.
